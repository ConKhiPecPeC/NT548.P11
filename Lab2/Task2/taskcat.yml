project:
  name: MyCloudFormationTests
  regions:
    - us-east-1
  # Thêm các khu vực bạn muốn kiểm tra
  s3_bucket: my-taskcat-bucket  # Tạo một bucket S3 để lưu trữ tài nguyên tạm thời của Taskcat
  
tests:
  my-test:
    template: group_19.yaml
    parameters:
      InstanceType: t2.micro
      KeyName: cloudfins
